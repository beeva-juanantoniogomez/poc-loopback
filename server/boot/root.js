'use strict';

module.exports = function(server) {
  // Uncomment this lines to install a `/` route that returns server status:
  //
  // var router = server.loopback.Router();
  // router.get('/', server.loopback.status());
  // server.use(router);
};
